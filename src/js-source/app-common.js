/**
 * Common application utilities and functions
 *
 * Fail-fast policy: No global message UI, no fallbacks.
 * Pages must surface errors via their own reactive state
 * (e.g. `error`, `wifiScan.error`, `validation.errors`).
 */

(function () {
  // Intentionally empty: no globals exposed.
})();
